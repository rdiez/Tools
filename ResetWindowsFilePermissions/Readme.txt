
This script resets file permissions under a given directory on a Windows system.

If your Windows system uses a language other than English, you will have to edit the script
manually, see the comment next to variable YES_STRING.

You may need to run this script inside a command prompt with elevated
privileges ("Run as Administrator").
